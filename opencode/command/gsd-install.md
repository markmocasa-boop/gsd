---
description: Install GSD OpenCode assets into this project
agent: build
---

Install the GSD OpenCode assets into this repository by calling the `gsd_install` tool.

If $ARGUMENTS contains "force", call the tool with `force: true`. Otherwise call it with `force: false`.

After the tool runs, confirm success and list the created paths.
