---
description: Analyze codebase with parallel mapper agents to produce .planning/codebase/ documents
agent: build
---

This runs the Claude prompt from `commands/gsd/map-codebase.md` so OpenCode stays aligned with the existing workflow.

@commands/gsd/map-codebase.md
