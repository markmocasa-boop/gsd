---
description: Insert urgent work as decimal phase (e.g., 72.1) between existing phases
agent: build
---

This runs the Claude prompt from `commands/gsd/insert-phase.md` so OpenCode stays aligned with the existing workflow.

@commands/gsd/insert-phase.md
