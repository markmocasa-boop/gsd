---
description: Remove a future phase from roadmap and renumber subsequent phases
agent: build
---

This runs the Claude prompt from `commands/gsd/remove-phase.md` so OpenCode stays aligned with the existing workflow.

@commands/gsd/remove-phase.md
