---
phase: 06-documentation
plan: 02
type: execute
depends_on: []
files_modified: [get-shit-done/examples/hello-world/, get-shit-done/examples/git-stats/, get-shit-done/examples/neo4j-knowledge-graph/]
---

<objective>
Create three working example plugins demonstrating simple, medium, and complex plugin patterns.

Purpose: Provide copy-paste-ready examples that developers can use as starting points for their own plugins.
Output: Three complete, installable example plugins in `get-shit-done/examples/`
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Reference for plugin structure:**
@get-shit-done/references/plugin-format.md

**Examples already in plugin-format.md to implement:**
- hello-world (minimal, lines 155-234)
- git-stats (simple command, lines 712-737)
- neo4j-knowledge-graph (complex with services, lines 239-380)

**Tech stack:** Plugin markdown files, JSON manifests, Docker Compose
**Established patterns:** Plugin structure from plugin-format.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hello-world example plugin (simple)</name>
  <files>get-shit-done/examples/hello-world/plugin.json, get-shit-done/examples/hello-world/README.md, get-shit-done/examples/hello-world/commands/greet.md</files>
  <action>
Create the minimal "hello-world" plugin exactly as specified in plugin-format.md:

**Directory structure:**
```
get-shit-done/examples/hello-world/
├── plugin.json
├── README.md
└── commands/
    └── greet.md
```

**plugin.json:** Use the exact example from plugin-format.md minimal_plugin_example section (lines 169-184).

**README.md:** Use the exact example from plugin-format.md (lines 189-212).

**commands/greet.md:** Use the exact example from plugin-format.md (lines 215-228).

This demonstrates:
- Minimum viable plugin
- Single command
- Proper namespacing
  </action>
  <verify>ls get-shit-done/examples/hello-world/ shows plugin.json, README.md, commands/greet.md</verify>
  <done>hello-world plugin has all 3 files with correct content matching plugin-format.md examples</done>
</task>

<task type="auto">
  <name>Task 2: Create git-stats example plugin (medium)</name>
  <files>get-shit-done/examples/git-stats/plugin.json, get-shit-done/examples/git-stats/README.md, get-shit-done/examples/git-stats/commands/summary.md, get-shit-done/examples/git-stats/commands/contributors.md</files>
  <action>
Create a "git-stats" plugin with two practical commands:

**Directory structure:**
```
get-shit-done/examples/git-stats/
├── plugin.json
├── README.md
└── commands/
    ├── summary.md
    └── contributors.md
```

**plugin.json:** Based on the git-stats example in plugin-format.md (lines 712-737), add full structure:
```json
{
  "name": "git-stats",
  "version": "1.0.0",
  "description": "Git statistics for GSD projects",
  "author": "GSD Examples",
  "license": "MIT",
  "gsd": {
    "minVersion": "1.4.0",
    "commands": [
      {
        "name": "git-stats:summary",
        "file": "commands/summary.md",
        "description": "Show commit statistics for the current repository"
      },
      {
        "name": "git-stats:contributors",
        "file": "commands/contributors.md",
        "description": "List contributors with commit counts"
      }
    ]
  }
}
```

**commands/summary.md:**
```markdown
---
name: git-stats:summary
description: Show commit statistics for the current repository
allowed-tools: [Bash]
---

<process>
Generate a summary of git commit statistics for the current repository.

Run these commands and format the output:
1. Total commits: `git rev-list --count HEAD`
2. Commits this week: `git rev-list --count --since="1 week ago" HEAD`
3. Most active day: `git log --format='%ad' --date=format:'%A' | sort | uniq -c | sort -rn | head -1`
4. First commit date: `git log --reverse --format='%ai' | head -1`

Present the results in a formatted summary.
</process>
```

**commands/contributors.md:**
```markdown
---
name: git-stats:contributors
description: List contributors with commit counts
allowed-tools: [Bash]
---

<process>
List all contributors to the repository sorted by commit count.

Run: `git shortlog -sn --all`

Format the output as a table showing:
- Contributor name
- Number of commits
- Percentage of total

Include a total at the bottom.
</process>
```

**README.md:** Document installation and both commands with example output.

This demonstrates:
- Multiple commands
- Practical utility (git integration)
- Proper allowed-tools declaration
  </action>
  <verify>ls get-shit-done/examples/git-stats/ shows plugin.json, README.md, commands/summary.md, commands/contributors.md</verify>
  <done>git-stats plugin has 4 files with functional git statistic commands</done>
</task>

<task type="auto">
  <name>Task 3: Create neo4j-knowledge-graph example plugin (complex)</name>
  <files>get-shit-done/examples/neo4j-knowledge-graph/plugin.json, get-shit-done/examples/neo4j-knowledge-graph/README.md, get-shit-done/examples/neo4j-knowledge-graph/commands/query.md, get-shit-done/examples/neo4j-knowledge-graph/commands/status.md, get-shit-done/examples/neo4j-knowledge-graph/hooks/post-research.md, get-shit-done/examples/neo4j-knowledge-graph/docker/docker-compose.yml</files>
  <action>
Create the full "neo4j-knowledge-graph" plugin from plugin-format.md complex_plugin_example:

**Directory structure:**
```
get-shit-done/examples/neo4j-knowledge-graph/
├── plugin.json
├── README.md
├── commands/
│   ├── query.md
│   └── status.md
├── hooks/
│   └── post-research.md
└── docker/
    └── docker-compose.yml
```

**plugin.json:** Use the complex example from plugin-format.md (lines 259-306), but simplify by removing the agent entry (agents require more infrastructure).

**docker/docker-compose.yml:** Use the exact example from plugin-format.md (lines 309-329).

**commands/query.md:** Use the exact example from plugin-format.md (lines 332-352).

**commands/status.md:** Create a status command:
```markdown
---
name: neo4j:status
description: Check Neo4j service status and connection
allowed-tools: [Bash]
---

<process>
Check the status of the Neo4j knowledge graph service.

1. Check if Docker container is running:
   `docker ps --filter "name=gsd-neo4j" --format "{{.Status}}"`

2. If running, test connection:
   `curl -s -o /dev/null -w "%{http_code}" http://localhost:7474`

3. Report status:
   - Container running/stopped
   - HTTP status (200 = healthy)
   - Port availability (7474 web, 7687 bolt)
</process>
```

**hooks/post-research.md:** Use the exact example from plugin-format.md (lines 355-370).

**README.md:** Comprehensive documentation including:
- Prerequisites (Docker)
- Installation
- Starting the service
- Command reference
- Hook behavior
- Troubleshooting

This demonstrates:
- Full plugin with Docker services
- Multiple commands
- Hook integration
- Service lifecycle
  </action>
  <verify>ls -R get-shit-done/examples/neo4j-knowledge-graph/ shows all 6 files across 3 directories</verify>
  <done>neo4j-knowledge-graph plugin has complete structure with commands, hooks, and docker-compose.yml</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] hello-world/ has 3 files, installable with `plugin install`
- [ ] git-stats/ has 4 files with working git commands
- [ ] neo4j-knowledge-graph/ has 6 files demonstrating full plugin capabilities
- [ ] All plugin.json files pass validation (name, version, description, author)
- [ ] Command namespacing is correct (pluginname:command format)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Three example plugins at increasing complexity
- Examples match patterns from plugin-format.md
- Phase 6 complete - documentation milestone finished
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-02-SUMMARY.md`
</output>
