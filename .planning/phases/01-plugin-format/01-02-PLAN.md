---
phase: 01-plugin-format
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [get-shit-done/templates/plugin/README.md, get-shit-done/references/plugin-format.md]
---

<objective>
Define the plugin folder structure and file conventions that plugins must follow.

Purpose: Establish where files live in a plugin and how they map to GSD's existing structure. This enables the installer (Phase 2) to know where to copy files.

Output:
- `get-shit-done/templates/plugin/README.md` - Plugin template README explaining structure
- Updated `get-shit-done/references/plugin-format.md` - Add folder structure section
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-plugin-format/01-01-SUMMARY.md

**Existing GSD structure (from STRUCTURE.md):**
- `commands/gsd/*.md` - Slash commands
- `agents/*.md` - Subagent definitions
- `get-shit-done/workflows/*.md` - Workflows
- `get-shit-done/templates/*.md` - Templates
- `get-shit-done/references/*.md` - Reference docs

**Plugin must mirror this:**
- `commands/*.md` - Plugin commands (installed to ~/.claude/commands/{plugin-name}/)
- `agents/*.md` - Plugin agents (installed to ~/.claude/agents/)
- `workflows/*.md` - Plugin workflows (installed to ~/.claude/{plugin-name}/workflows/)
- `templates/*.md` - Plugin templates
- `references/*.md` - Plugin references
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define plugin folder structure</name>
  <files>get-shit-done/templates/plugin/README.md</files>
  <action>
Create `get-shit-done/templates/plugin/README.md` as a template for plugin READMEs that documents:

**Required structure:**
```
my-plugin/
├── plugin.json          # Manifest (required)
├── README.md            # Plugin documentation (required)
├── commands/            # Commands (optional)
│   └── *.md            # Each file = one slash command
├── agents/              # Subagents (optional)
│   └── *.md            # Each file = one agent
├── workflows/           # Workflows (optional)
│   └── *.md            # Referenced by commands
├── templates/           # Templates (optional)
│   └── *.md            # File templates
├── references/          # Reference docs (optional)
│   └── *.md            # Implementation guides
├── docker/              # Services (optional, Phase 5)
│   ├── docker-compose.yml
│   └── Dockerfile
└── hooks/               # Lifecycle hooks (optional)
    └── *.md            # Hook handlers
```

**Naming conventions:**
- Plugin name: kebab-case (e.g., `neo4j-knowledge-graph`)
- Commands: `{plugin-name}:{command}` namespace (e.g., `/neo4j:query`)
- Agents: `{plugin-name}-{agent}` (e.g., `neo4j-kg-indexer`)
- All markdown files: kebab-case.md

**Installation mapping:**
| Plugin Path | Installed To |
|-------------|--------------|
| commands/*.md | ~/.claude/commands/{plugin-name}/*.md |
| agents/*.md | ~/.claude/agents/*.md |
| workflows/*.md | ~/.claude/{plugin-name}/workflows/*.md |
| templates/*.md | ~/.claude/{plugin-name}/templates/*.md |
| references/*.md | ~/.claude/{plugin-name}/references/*.md |
| hooks/*.md | ~/.claude/{plugin-name}/hooks/*.md |
| docker/* | ~/.claude/{plugin-name}/docker/* |

The README template should include:
1. Plugin name and description placeholders
2. Installation command placeholder
3. Available commands section
4. Configuration section (if applicable)
5. Usage examples section

Format as a template with [PLACEHOLDER] markers that plugin authors replace.
  </action>
  <verify>cat get-shit-done/templates/plugin/README.md | head -40</verify>
  <done>Plugin README template exists with folder structure documented</done>
</task>

<task type="auto">
  <name>Task 2: Add folder structure to reference docs</name>
  <files>get-shit-done/references/plugin-format.md</files>
  <action>
Update `get-shit-done/references/plugin-format.md` (created in 01-01) to add:

**New section: Folder Structure**
- Directory layout diagram
- Purpose of each directory
- Required vs optional directories
- Installation mapping table

**New section: File Conventions**
- Naming patterns for commands, agents, workflows
- Command namespacing rules
- Path references within plugin files (use relative paths)

**New section: Minimal Plugin Example**
Show the simplest possible plugin (single command):
```
hello-world/
├── plugin.json
├── README.md
└── commands/
    └── hello.md
```

With complete file contents for each.

**New section: Complex Plugin Example**
Show structure for Neo4j knowledge graph plugin (from PROJECT.md):
```
neo4j-knowledge-graph/
├── plugin.json
├── README.md
├── commands/
│   ├── query.md
│   └── status.md
├── agents/
│   └── neo4j-kg-indexer.md
├── workflows/
│   └── research-capture.md
├── hooks/
│   └── post-research.md
└── docker/
    └── docker-compose.yml
```
  </action>
  <verify>grep -c "Folder Structure" get-shit-done/references/plugin-format.md (section exists)</verify>
  <done>plugin-format.md updated with folder structure and file conventions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `get-shit-done/templates/plugin/README.md` exists as template
- [ ] `get-shit-done/references/plugin-format.md` has folder structure section
- [ ] Minimal and complex examples included
- [ ] Installation mapping is clear for Phase 2 to implement
</verification>

<success_criteria>

- Plugin folder structure fully defined
- Naming conventions documented
- Examples show both simple and complex plugins
- Phase 2 (installation) has clear mapping to implement
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-format/01-02-SUMMARY.md`
</output>
